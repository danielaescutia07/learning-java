package main.java.com.daniela;

//for an interface, we need to remove the "abstract" keyword and change "class" to "interface"
//with interfaces, we are NOT allowed to have properties or method with implementations
//we are only allowed to have abstract methods
    public interface AnimalInterface {

    void makeSound();
    String getName();


}
